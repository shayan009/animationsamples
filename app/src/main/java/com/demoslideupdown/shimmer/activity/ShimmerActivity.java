package com.demoslideupdown.shimmer.activity;import android.content.Context;import android.support.v7.app.AppCompatActivity;import android.os.Bundle;import android.support.v7.widget.LinearLayoutManager;import android.view.View;import com.demoslideupdown.R;import com.demoslideupdown.shimmer.adapter.CardAdapter;import com.demoslideupdown.shimmer.ui.custum_views.recycler_view_shimmer.CustomRecyclerView;import com.demoslideupdown.utils.DataUtils;import butterknife.BindView;import butterknife.ButterKnife;import butterknife.Unbinder;public class ShimmerActivity extends AppCompatActivity {    private Context context;    private Unbinder unbinder;    private CardAdapter cardAdapter;    @BindView(R.id.customRecyclerView)    CustomRecyclerView customRecyclerView;    @Override    protected void onCreate (Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_shimmer);        context=this;        unbinder= ButterKnife.bind(this);        customRecyclerView.setHasFixedSize(false);        customRecyclerView.setLayoutManager(new LinearLayoutManager(context));        cardAdapter = new CardAdapter();        customRecyclerView.setAdapter(cardAdapter);        customRecyclerView.showShimmerAdapter();        customRecyclerView.postDelayed(new Runnable() {            @Override            public void run() {                loadCards();            }        }, 3000);    }    private void loadCards() {        //customRecyclerView.setVisibility(View.GONE);        cardAdapter.setCards(DataUtils.getListCards(getResources()));     //   cardAdapter.notifyDataSetChanged();        customRecyclerView.hideShimmerAdapter();    }    @Override    protected void onDestroy () {        super.onDestroy();        unbinder.unbind();    }}